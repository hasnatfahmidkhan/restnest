model PropertyAmenity {
    id         String @id @default(uuid())
    amenityId  String
    propertyId String

    // references 
    amenity  Amenity  @relation(fields: [amenityId], references: [id])
    property Property @relation(fields: [propertyId], references: [id])

    @@map("propertyamenities")
}
